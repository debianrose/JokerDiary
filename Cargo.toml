[package]
name = "jokerdiary"
version = "0.1.0"
edition = "2021"

[dependencies]
tauri = { version = "1.0", features = ["api-all"] }
serde = { version = "1.0", features = ["derive"] }

[build-dependencies]
tauri-build = { version = "1.0" }
